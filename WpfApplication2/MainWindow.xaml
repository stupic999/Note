<Window x:Class="WpfApplication2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication2"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <!--定义格子-->
            <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <!--内容-->
        <Rectangle Grid.Column="1" Fill="Gray">        </Rectangle>
        <StackPanel Grid.Column="1">
            <Button Margin="10" x:Name="OpenBtn" Click="OpenBtn_Click">open</Button>
            <Button Margin="10" Click="SaveBtn_Click" x:Name="SaveBtn">save</Button>
        </StackPanel>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <TextBox TextWrapping="Wrap" AcceptsReturn="True" x:Name="Textarea"></TextBox>
        </ScrollViewer>
    </Grid>
</Window>
